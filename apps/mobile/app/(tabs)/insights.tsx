import { Text } from "react-native";
import { Screen } from "../../src/ui/screen";

export default function InsightsPlaceholder() {
  return (
    <Screen title="Insights">
      <Text>Heatmap and metrics land in the insights slice.</Text>
    </Screen>
  );
}
