## Landing Page Follow-ups

### Try It Now Sandbox

- Add a `Try it now` CTA on the landing page that routes into a sandboxed app session.
- Start the sandbox with one seeded demo user profile and pre-populated planner data.
- Keep the experience fully editable for exploration, but route writes to a non-persistent sandbox layer.
- Reset sandbox state on session end or expiration so each visit starts clean.
- Validate auth/session boundaries so sandbox access does not overlap with real user accounts.
